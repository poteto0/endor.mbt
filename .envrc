# direnv: enter the flake's shell on `cd`, leave it on the way out.
# Optional — `nix develop` does the same thing by hand. `direnv allow` once.
#
# `nix-direnv` caches the shell so this is not a nix evaluation per prompt:
# https://github.com/nix-community/nix-direnv
use flake
