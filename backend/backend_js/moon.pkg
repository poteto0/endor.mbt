import {
  "poteto0/endor/types",
  "poteto0/endor/ffi/js",
  "poteto0/endor/provider",
  "poteto0/endor/provider/browser",
  "moonbitlang/async",
}

// Everything here is a test file, so the package publishes no interface at all.
// It is `js`-only because it drives `BrowserProvider`, and it is the one
// package that talks to a live node — see `docs/e2e.md`.

supported_targets = "js"
